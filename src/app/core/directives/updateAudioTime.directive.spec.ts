/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UpdateAudioTimeDirective } from './updateAudioTime.directive';

describe('Directive: UpdateAudioTime', () => {
  it('should create an instance', () => {
    const directive = new UpdateAudioTimeDirective();
    expect(directive).toBeTruthy();
  });
});
